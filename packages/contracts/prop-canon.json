{
  "_doc": "Canonical overlay for the prop glossary. This is the HAND-MAINTAINED half: it declares which props are shared axes, their canonical values, the value glossary, and the disposition of each known divergence. The MEASURED half is extracted from component source. Edit this file to change the canon; run `pnpm prop-map` to regenerate .ai/maps/prop-map.{json,md}.",
  "_canon": "packages/contracts/components/README.md §2 is the prose statement of this registry. Keep the two in step — the axes and values here must match §2, and §2 points at the generated map for measured usage.",
  "_starter": "These axes ship as a STARTING POINT, not a decision. They are the vocabulary a design proposal is written in before any component exists. Changing them is expected work for a new system — but change them HERE and regenerate, never by editing the generated map.",

  "conventions": {
    "propCase": "camelCase",
    "valueCase": "kebab-case",
    "booleanNaming": "A bare adjective or state — disabled, loading, fullWidth. Never is*/has*/show*: the prop already reads as a predicate at the call site (<Button disabled />), and the prefix only adds noise."
  },

  "axes": {
    "size": {
      "concept": "One ordered scale, shared with the token ladder (--juro-space-*, --juro-font-size-*). A component exposes a contiguous subset of it, never a gap.",
      "values": ["xs", "s", "m", "l", "xl"],
      "default": "m"
    },
    "hierarchy": {
      "concept": "How much emphasis an action carries. Orthogonal to `variant`, which chooses the colour, and distinct from a paint treatment. Actions have a rank; surfaces do not — so this axis is for interactive components only.",
      "values": ["primary", "secondary", "tertiary"],
      "default": "primary"
    },
    "variant": {
      "concept": "Semantic intent, which selects the colour role. Orthogonal to `hierarchy`, which decides how much emphasis is applied.",
      "values": ["neutral", "brand", "success", "warning", "danger"],
      "default": "neutral"
    },
    "orientation": {
      "concept": "Layout axis along which the component and its children flow.",
      "values": ["horizontal", "vertical"]
    },
    "placement": {
      "concept": "Where the component sits relative to its anchor or host. Two profiles — a component picks one whole profile, never a mix of the two.",
      "profiles": {
        "overlay": {
          "note": "Full floating-element vocabulary for anchored overlays.",
          "values": [
            "top",
            "top-start",
            "top-end",
            "bottom",
            "bottom-start",
            "bottom-end",
            "left",
            "left-start",
            "left-end",
            "right",
            "right-start",
            "right-end"
          ]
        },
        "edge": {
          "note": "For things that dock to a side of their container.",
          "values": ["top", "bottom", "start", "end"]
        }
      }
    }
  },

  "glossary": {
    "_doc": "One spelling, one meaning. A new value is added here in the same change that introduces it — otherwise the second person to need it coins a synonym.",
    "neutral": "Default, low-emphasis intent. Canonical — never `default` or `base`.",
    "brand": "Carries the system's primary brand colour. Canonical — never `primary` as a *colour* (primary is a hierarchy rank).",
    "danger": "Error or destructive. Canonical — never `error`, `critical` or `red`.",
    "success": "Confirmation or a completed state. Canonical — never `positive` or `green`.",
    "warning": "Needs attention but is not an error. Canonical — never `caution` or `yellow`.",
    "xs": "Extra small. Canonical — never `xsmall` or `tiny`.",
    "s": "Small. Canonical — never `small` or `sm`.",
    "m": "Medium, the default step. Canonical — never `medium` or `md`.",
    "l": "Large. Canonical — never `large` or `lg`.",
    "xl": "Extra large. Canonical — never `xlarge` or `huge`.",
    "start": "Logical inline start — left in LTR, right in RTL. Preferred over `left`.",
    "end": "Logical inline end — right in LTR, left in RTL. Preferred over `right`.",
    "horizontal": "Flows along the inline axis.",
    "vertical": "Flows along the block axis."
  },

  "_dispositionDoc": "How to record a measured divergence from the canon above. `fix` = a defect, someone should change the component. `review` = needs a decision, nobody has made it yet. `accepted` = intentional, with the reason. `legacy` = tolerated on an existing component, never introduced in new work. A divergence with NO entry here reports as `unreviewed`, which is what stops a new one landing silently. Keys are `<Component>.<propName>`. The generator FAILS on a disposition naming a component or prop that does not exist, so this list cannot quietly rot.",
  "dispositions": {}
}
