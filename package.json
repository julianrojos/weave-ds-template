{
  "name": "ds-template",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "init-ds": "node scripts/init-ds.mjs",
    "dev": "pnpm --filter sandbox dev",
    "dev:storybook": "pnpm --filter storybook dev",
    "build": "pnpm build:tokens && pnpm build:react",
    "build:tokens": "pnpm --filter @juro/tokens build",
    "build:react": "pnpm --filter @juro/react build",
    "tokens:watch": "pnpm --filter @juro/tokens watch",
    "contract": "node packages/react/scripts/contract.mjs",
    "prop-map": "node packages/react/scripts/build-prop-map.mjs",
    "prop-map:check": "node packages/react/scripts/build-prop-map.mjs --check",
    "adr-index": "node scripts/adr-index.mjs",
    "adr-index:check": "node scripts/adr-index.mjs --check",
    "report:paints": "node packages/react/scripts/report-paints.mjs",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm verify:tokens && pnpm verify:contract && pnpm prop-map:check && pnpm adr-index:check && pnpm verify:docs && pnpm verify:figma && pnpm build && pnpm test",
    "verify:contract": "node packages/react/scripts/verify-contract.mjs",
    "verify:figma": "node scripts/verify-figma.mjs",
    "verify:tokens": "node scripts/verify-tokens.mjs",
    "verify:docs": "node scripts/verify-docs.mjs",
    "typecheck": "tsc --noEmit -p packages/react/tsconfig.json && tsc --noEmit -p apps/sandbox/tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "clean": "node -e \"for (const p of ['packages/tokens/build','packages/react/dist','node_modules/.vite']) require('fs').rmSync(p,{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ajv": "^8.17.1",
    "eslint": "^10.9.1",
    "prettier": "^3.4.2",
    "typescript": "~5.9.3",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "//typescript": "Tilde-pinned, and THE REASON IT WAS PINNED IS GONE. It protected react-docgen-typescript, which silently stopped classifying VariantProps-derived props as enums under typescript@6 — value arrays came back empty and every contract answer thinned out with nothing warning you. That library and the extraction readers that used it were deleted on 2026-09-03, because components are generated from contracts and there is no hand-written source left to read. So this pin now defends nothing measurable. It is left in place because widening a toolchain range is a deliberate act with its own blast radius, not a side effect of deleting dead code — but whoever wants typescript 6 should now simply take it."
}
